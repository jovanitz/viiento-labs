{
  "name": "bison-manager",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/bison-manager/src",
  "projectType": "application",
  "tags": ["layer:app"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint {projectRoot}"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p apps/bison-manager/tsconfig.app.json --noEmit"
      }
    }
  }
}
